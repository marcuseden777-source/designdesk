{
  "buildCommand": "cd app/frontend && npm install && npx expo export --platform web",
  "outputDirectory": "app/frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
